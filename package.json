{
    "scripts": {
        "dev": "wrangler dev src/index.ts",
        "deploy": "wrangler deploy --minify src/index.ts",
        "init": "node --env-file=config.env scripts/init.js",
        "test": "vitest",
        "test:run": "vitest run",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "hono": "^4.11.8"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20260207.0",
        "@types/node": "^25.2.1",
        "typescript": "^5.9.3",
        "vitest": "^4.0.18",
        "wrangler": "^4.63.0"
    }
}
